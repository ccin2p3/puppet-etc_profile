---
lookup_options:
  etc_profile::config:
    merge:
      strategy: deep
      merge_hash_arrays: false
etc_profile::config:
  file:
    epp: 'etc_profile/site.epp'
  dir:
    ensure: directory
    purge: true
